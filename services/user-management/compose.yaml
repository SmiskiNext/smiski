services:
    postgres:
        image: 'postgres:latest'
        environment:
            - 'POSTGRES_DB=zms_users'
            - 'POSTGRES_PASSWORD=zms_secret'
            - 'POSTGRES_USER=zms_user'
        ports:
            - '5432:5432'

    user-management:
        build: .
        environment:
            - 'ZMS_FIREBASE_PROJECT_ID=${ZMS_FIREBASE_PROJECT_ID:-}'
            - 'ZMS_GOOGLE_APPLICATION_CREDENTIALS=/secrets/firebase-service-account.json'
        volumes:
            - '${ZMS_FIREBASE_SERVICE_ACCOUNT_PATH:-/dev/null}:/secrets/firebase-service-account.json:ro'
        depends_on:
            - postgres
            - kafka

    kafka:
        image: confluentinc/cp-kafka:7.8.0
        hostname: kafka
        container_name: kafka
        ports:
            - '9092:9092'
        environment:
            KAFKA_NODE_ID: 1
            KAFKA_PROCESS_ROLES: broker,controller
            KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093
            KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
            KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,CONTROLLER:PLAINTEXT
            KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
            KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
            KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
            KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
            KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
            KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
            KAFKA_AUTO_CREATE_TOPICS_ENABLE: 'true'
            CLUSTER_ID: 'MkU3OEVBNTcwNTJENDM2Qk'
        volumes:
            - kafka-data:/var/lib/kafka/data

    kafka-ui:
        image: provectuslabs/kafka-ui:latest
        container_name: kafka-ui
        ports:
            - '8090:8080'
        environment:
            KAFKA_CLUSTERS_0_NAME: local
            KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:9092
        depends_on:
            - kafka

volumes:
    kafka-data:
