apiVersion: v1
kind: Secret
metadata:
    name: rustfs-credentials
    namespace: rustfs-system
type: Opaque
stringData:
    access-key: 'MUST_CHANGE_IN_PRODUCTION'
    secret-key: 'MUST_CHANGE_IN_PRODUCTION'
