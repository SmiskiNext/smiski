# Generated per-service OpenAPI specs (springdoc formatting is canonical)
services/*/openapi.yaml

# Dependency lockfiles (owned by package managers)
pnpm-lock.yaml
**/package-lock.json

# Nested Custom UI npm package (own react-scripts toolchain)
app/static/hello-world/**

# Local OpenCode tooling (not part of the repo build)
.opencode/**
