version: '2'

linters:
    default: standard
    enable:
        - errcheck
        - govet
        - staticcheck
        - unused
        - ineffassign

formatters:
    enable:
        - gofmt

issues:
    max-issues-per-linter: 0
    max-same-issues: 0
