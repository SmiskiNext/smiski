// This file is auto-generated by @hey-api/openapi-ts

import { client } from './client.gen.js';
import type { Client, Options as Options2, TDataShape } from './client/index.js';
import type { AcceptInvitationData, AcceptInvitationErrors, AcceptInvitationResponses, AcceptJoinRequestsData, AcceptJoinRequestsErrors, AcceptJoinRequestsResponses, AddInviteesData, AddInviteesErrors, AddInviteesResponses, BatchDeleteData, BatchDeleteErrors, BatchDeleteInviteesData, BatchDeleteInviteesErrors, BatchDeleteInviteesResponses, BatchDeleteResponses, CancelData, CancelErrors, CancelResponses, CreateInstantData, CreateInstantErrors, CreateInstantResponses, DeclineInvitationData, DeclineInvitationErrors, DeclineInvitationResponses, DeclineJoinRequestsData, DeclineJoinRequestsErrors, DeclineJoinRequestsResponses, DeleteData, DeleteErrors, DeleteResponses, EndData, EndErrors, EndResponses, GetData, GetErrors, GetResponses, HandleInboundEmailData, HandleInboundEmailErrors, HandleInboundEmailResponses, JoinData, JoinErrors, JoinResponses, ListData, ListErrors, ListPendingJoinRequestsData, ListPendingJoinRequestsErrors, ListPendingJoinRequestsResponses, ListResponses, ReceiveData, ReceiveErrors, ReceiveResponses, RegisterData, RegisterErrors, RegisterResponses, ScheduleData, ScheduleErrors, ScheduleResponses, SubscribeData, SubscribeErrors, SubscribeRequestData, SubscribeRequestErrors, SubscribeRequestResponses, SubscribeResponses, TentativeInvitationData, TentativeInvitationErrors, TentativeInvitationResponses, UninstallData, UninstallErrors, UninstallResponses, UpdateData, UpdateErrors, UpdateResponses, UpdateSettingsData, UpdateSettingsErrors, UpdateSettingsResponses } from './types.gen.js';
import { zAcceptInvitationData, zAcceptInvitationResponse, zAcceptJoinRequestsData, zAcceptJoinRequestsResponse, zAddInviteesData, zAddInviteesResponse, zBatchDeleteData, zBatchDeleteInviteesData, zBatchDeleteInviteesResponse, zBatchDeleteResponse, zCancelData, zCancelResponse, zCreateInstantData, zCreateInstantResponse, zDeclineInvitationData, zDeclineInvitationResponse, zDeclineJoinRequestsData, zDeclineJoinRequestsResponse, zDeleteData, zDeleteResponse, zEndData, zEndResponse, zGetData, zGetResponse, zHandleInboundEmailData, zJoinData, zJoinResponse, zListData, zListPendingJoinRequestsData, zListPendingJoinRequestsResponse, zListResponse, zReceiveData, zRegisterData, zRegisterResponse, zScheduleData, zScheduleResponse, zSubscribeData, zSubscribeRequestData, zSubscribeRequestResponse, zSubscribeResponse, zTentativeInvitationData, zTentativeInvitationResponse, zUninstallData, zUninstallResponse, zUpdateData, zUpdateResponse, zUpdateSettingsData, zUpdateSettingsResponse } from './zod.gen.js';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = Options2<TData, ThrowOnError> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Uninstall a tenant
 *
 * Records a Forge app uninstall. Marks the tenant as UNINSTALLED and schedules purge. Idempotent for already-uninstalled tenants.
 */
export const uninstall = <ThrowOnError extends boolean = false>(options: Options<UninstallData, ThrowOnError>) => (options.client ?? client).delete<UninstallResponses, UninstallErrors, ThrowOnError>({
    requestValidator: async (data) => await zUninstallData.parseAsync(data),
    responseValidator: async (data) => await zUninstallResponse.parseAsync(data),
    url: '/api/{version}/tenants',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Register a tenant
 *
 * Registers a new tenant installation or acknowledges a reinstall/redelivery. Returns 201 on first install with a Location header, 200 on reinstall.
 */
export const register = <ThrowOnError extends boolean = false>(options: Options<RegisterData, ThrowOnError>) => (options.client ?? client).post<RegisterResponses, RegisterErrors, ThrowOnError>({
    requestValidator: async (data) => await zRegisterData.parseAsync(data),
    responseValidator: async (data) => await zRegisterResponse.parseAsync(data),
    url: '/api/{version}/tenants',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a meeting
 *
 * Soft-deletes a meeting as its host. Running meetings must be ended first, and already-deleted meetings are treated as not found.
 */
export const delete_ = <ThrowOnError extends boolean = false>(options: Options<DeleteData, ThrowOnError>) => (options.client ?? client).delete<DeleteResponses, DeleteErrors, ThrowOnError>({
    requestValidator: async (data) => await zDeleteData.parseAsync(data),
    responseValidator: async (data) => await zDeleteResponse.parseAsync(data),
    url: '/api/{version}/meetings/{id}',
    ...options
});

/**
 * Get a meeting with its people
 *
 * Returns a single meeting in the caller's tenant together with its active invitee list and its distinct joined-participant list. Any authenticated tenant member may read the meeting; access is not restricted to the host.
 */
export const get = <ThrowOnError extends boolean = false>(options: Options<GetData, ThrowOnError>) => (options.client ?? client).get<GetResponses, GetErrors, ThrowOnError>({
    requestValidator: async (data) => await zGetData.parseAsync(data),
    responseValidator: async (data) => await zGetResponse.parseAsync(data),
    url: '/api/{version}/meetings/{id}',
    ...options
});

/**
 * Update meeting information
 *
 * Updates a meeting's information as its host: title, description, issue link, zone ID, and time range. Mutable while scheduled or running; zone ID and time range are mutable only while scheduled. To replace the settings block, use PUT /meetings/{id}/settings instead.
 */
export const update = <ThrowOnError extends boolean = false>(options: Options<UpdateData, ThrowOnError>) => (options.client ?? client).put<UpdateResponses, UpdateErrors, ThrowOnError>({
    requestValidator: async (data) => await zUpdateData.parseAsync(data),
    responseValidator: async (data) => await zUpdateResponse.parseAsync(data),
    url: '/api/{version}/meetings/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Replace meeting settings
 *
 * Replaces the entire meeting settings block as its host: admission policy, participant limit, and media/chat permissions. Permitted while the meeting is SCHEDULED or RUNNING. When the change affects media permissions, every connected non-host participant's LiveKit publish permission is updated in real time on a best-effort basis; the host is never affected.
 */
export const updateSettings = <ThrowOnError extends boolean = false>(options: Options<UpdateSettingsData, ThrowOnError>) => (options.client ?? client).put<UpdateSettingsResponses, UpdateSettingsErrors, ThrowOnError>({
    requestValidator: async (data) => await zUpdateSettingsData.parseAsync(data),
    responseValidator: async (data) => await zUpdateSettingsResponse.parseAsync(data),
    url: '/api/{version}/meetings/{id}/settings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Receive a LiveKit webhook
 *
 * Verifies the LiveKit signed-JWT payload against the raw body and enqueues the decoded event for asynchronous processing.
 */
export const receive = <ThrowOnError extends boolean = false>(options: Options<ReceiveData, ThrowOnError>) => (options.client ?? client).post<ReceiveResponses, ReceiveErrors, ThrowOnError>({
    requestValidator: async (data) => await zReceiveData.parseAsync(data),
    url: '/api/{version}/webhooks/livekit',
    ...options,
    headers: {
        'Content-Type': 'application/webhook+json',
        ...options.headers
    }
});

/**
 * List tenant meetings
 *
 * Lists meetings in the caller's tenant with optional creator, status, issue, and text filters, two sort modes, and opaque keyset pagination. The request body is optional; an empty body lists with defaults.
 */
export const list = <ThrowOnError extends boolean = false>(options: Options<ListData, ThrowOnError>) => (options.client ?? client).post<ListResponses, ListErrors, ThrowOnError>({
    requestValidator: async (data) => await zListData.parseAsync(data),
    responseValidator: async (data) => await zListResponse.parseAsync(data),
    url: '/api/{version}/meetings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a scheduled meeting
 *
 * Creates a new SCHEDULED meeting with a future time range. The meeting stays in SCHEDULED status and no LiveKit token is issued.
 */
export const schedule = <ThrowOnError extends boolean = false>(options: Options<ScheduleData, ThrowOnError>) => (options.client ?? client).post<ScheduleResponses, ScheduleErrors, ThrowOnError>({
    requestValidator: async (data) => await zScheduleData.parseAsync(data),
    responseValidator: async (data) => await zScheduleResponse.parseAsync(data),
    url: '/api/{version}/meetings:schedule',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create an instant meeting
 *
 * Creates a new INSTANT meeting, auto-starts it to RUNNING, registers invitees with invite tokens, and returns a meeting snapshot plus a LiveKit HOST access token.
 */
export const createInstant = <ThrowOnError extends boolean = false>(options: Options<CreateInstantData, ThrowOnError>) => (options.client ?? client).post<CreateInstantResponses, CreateInstantErrors, ThrowOnError>({
    requestValidator: async (data) => await zCreateInstantData.parseAsync(data),
    responseValidator: async (data) => await zCreateInstantResponse.parseAsync(data),
    url: '/api/{version}/meetings:instant',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Batch delete meetings
 *
 * Atomically soft-deletes a non-empty list of meetings as their host. The batch is all-or-nothing: if any meeting is not found, already deleted, running, or hosted by another account, no meeting is deleted.
 */
export const batchDelete = <ThrowOnError extends boolean = false>(options: Options<BatchDeleteData, ThrowOnError>) => (options.client ?? client).post<BatchDeleteResponses, BatchDeleteErrors, ThrowOnError>({
    requestValidator: async (data) => await zBatchDeleteData.parseAsync(data),
    responseValidator: async (data) => await zBatchDeleteResponse.parseAsync(data),
    url: '/api/{version}/meetings:batchDelete',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Join a meeting
 *
 * Joins a meeting as an authenticated account. Under ALLOW_ALL admission the caller is admitted immediately with status APPROVED, a LiveKit token, and the room name. Under MANUAL_APPROVAL a pending request is created with status PENDING and a requestId; both outcomes return 200.
 */
export const join = <ThrowOnError extends boolean = false>(options: Options<JoinData, ThrowOnError>) => (options.client ?? client).post<JoinResponses, JoinErrors, ThrowOnError>({
    requestValidator: async (data) => await zJoinData.parseAsync(data),
    responseValidator: async (data) => await zJoinResponse.parseAsync(data),
    url: '/api/{version}/meetings/{id}:join',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * End a running meeting
 *
 * Ends a RUNNING meeting as its host. Transitions the meeting to COMPLETED, closes all active participation logs, publishes a MeetingCompletedEvent through the transactional outbox, and requests best-effort deletion of the LiveKit room.
 */
export const end = <ThrowOnError extends boolean = false>(options: Options<EndData, ThrowOnError>) => (options.client ?? client).post<EndResponses, EndErrors, ThrowOnError>({
    requestValidator: async (data) => await zEndData.parseAsync(data),
    responseValidator: async (data) => await zEndResponse.parseAsync(data),
    url: '/api/{version}/meetings/{id}:end',
    ...options
});

/**
 * Cancel a meeting
 *
 * Cancels a SCHEDULED meeting as its host. The reason is always HOST_CANCELED. A MeetingCanceledEvent is published through the transactional outbox, carrying the active invitee list. Only SCHEDULED meetings can be canceled via this endpoint.
 */
export const cancel = <ThrowOnError extends boolean = false>(options: Options<CancelData, ThrowOnError>) => (options.client ?? client).post<CancelResponses, CancelErrors, ThrowOnError>({
    requestValidator: async (data) => await zCancelData.parseAsync(data),
    responseValidator: async (data) => await zCancelResponse.parseAsync(data),
    url: '/api/{version}/meetings/{id}:cancel',
    ...options
});

/**
 * Decline pending join requests
 *
 * Declines one or more pending join requests as the meeting host. Processing is best-effort per item: each submitted requestId yields a result entry with status DENIED (no token), or FAILED (carrying a machine-readable reason). Only the host may decline.
 */
export const declineJoinRequests = <ThrowOnError extends boolean = false>(options: Options<DeclineJoinRequestsData, ThrowOnError>) => (options.client ?? client).post<DeclineJoinRequestsResponses, DeclineJoinRequestsErrors, ThrowOnError>({
    requestValidator: async (data) => await zDeclineJoinRequestsData.parseAsync(data),
    responseValidator: async (data) => await zDeclineJoinRequestsResponse.parseAsync(data),
    url: '/api/{version}/meetings/{id}/join-requests:decline',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Accept pending join requests
 *
 * Accepts one or more pending join requests as the meeting host. Processing is best-effort per item: each submitted requestId yields a result entry with status APPROVED (carrying a LiveKit token and room name), or FAILED (carrying a machine-readable reason such as MEETING_FULL, JOIN_REQUEST_NOT_FOUND, or JOIN_REQUEST_EXPIRED). Capacity is enforced so the batch never exceeds maxParticipants. Only the host may accept.
 */
export const acceptJoinRequests = <ThrowOnError extends boolean = false>(options: Options<AcceptJoinRequestsData, ThrowOnError>) => (options.client ?? client).post<AcceptJoinRequestsResponses, AcceptJoinRequestsErrors, ThrowOnError>({
    requestValidator: async (data) => await zAcceptJoinRequestsData.parseAsync(data),
    responseValidator: async (data) => await zAcceptJoinRequestsResponse.parseAsync(data),
    url: '/api/{version}/meetings/{id}/join-requests:accept',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Add invitees to a meeting
 *
 * Adds one or more new invitees to a SCHEDULED meeting as its host. Each new account is created with status NEEDS_ACTION. The request is atomic: if any submitted accountId is already an active invitee the whole batch is rejected with 409 INVITEE_ALREADY_EXISTS.
 */
export const addInvitees = <ThrowOnError extends boolean = false>(options: Options<AddInviteesData, ThrowOnError>) => (options.client ?? client).post<AddInviteesResponses, AddInviteesErrors, ThrowOnError>({
    requestValidator: async (data) => await zAddInviteesData.parseAsync(data),
    responseValidator: async (data) => await zAddInviteesResponse.parseAsync(data),
    url: '/api/{version}/meetings/{id}/invitees',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Remove invitees from a meeting
 *
 * Removes one or more invitees from a SCHEDULED meeting as its host, by invitee id. The request is atomic: if any submitted id does not correspond to an active invitee of the meeting the whole batch is rejected with 404 INVITEE_NOT_FOUND.
 */
export const batchDeleteInvitees = <ThrowOnError extends boolean = false>(options: Options<BatchDeleteInviteesData, ThrowOnError>) => (options.client ?? client).post<BatchDeleteInviteesResponses, BatchDeleteInviteesErrors, ThrowOnError>({
    requestValidator: async (data) => await zBatchDeleteInviteesData.parseAsync(data),
    responseValidator: async (data) => await zBatchDeleteInviteesResponse.parseAsync(data),
    url: '/api/{version}/meetings/{id}/invitees:batchDelete',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Tentatively respond to a meeting invitation
 *
 * Marks the caller's own meeting invitation as tentative. The acting account, resolved from the account header, must own the target invitee. Returns the updated invitee snapshot on success.
 */
export const tentativeInvitation = <ThrowOnError extends boolean = false>(options: Options<TentativeInvitationData, ThrowOnError>) => (options.client ?? client).post<TentativeInvitationResponses, TentativeInvitationErrors, ThrowOnError>({
    requestValidator: async (data) => await zTentativeInvitationData.parseAsync(data),
    responseValidator: async (data) => await zTentativeInvitationResponse.parseAsync(data),
    url: '/api/{version}/meetings/{id}/invitees/{inviteeId}:tentative',
    ...options
});

/**
 * Decline a meeting invitation
 *
 * Declines the caller's own meeting invitation. The acting account, resolved from the account header, must own the target invitee. Returns the updated invitee snapshot on success.
 */
export const declineInvitation = <ThrowOnError extends boolean = false>(options: Options<DeclineInvitationData, ThrowOnError>) => (options.client ?? client).post<DeclineInvitationResponses, DeclineInvitationErrors, ThrowOnError>({
    requestValidator: async (data) => await zDeclineInvitationData.parseAsync(data),
    responseValidator: async (data) => await zDeclineInvitationResponse.parseAsync(data),
    url: '/api/{version}/meetings/{id}/invitees/{inviteeId}:decline',
    ...options
});

/**
 * Accept a meeting invitation
 *
 * Accepts the caller's own meeting invitation. The acting account, resolved from the account header, must own the target invitee. Returns the updated invitee snapshot on success.
 */
export const acceptInvitation = <ThrowOnError extends boolean = false>(options: Options<AcceptInvitationData, ThrowOnError>) => (options.client ?? client).post<AcceptInvitationResponses, AcceptInvitationErrors, ThrowOnError>({
    requestValidator: async (data) => await zAcceptInvitationData.parseAsync(data),
    responseValidator: async (data) => await zAcceptInvitationResponse.parseAsync(data),
    url: '/api/{version}/meetings/{id}/invitees/{inviteeId}:accept',
    ...options
});

/**
 * List pending join requests
 *
 * Returns a paginated list of PENDING join requests for a meeting with MANUAL_APPROVAL admission policy. Only the meeting host may call this endpoint. Use offset and pageSize query parameters for pagination.
 */
export const listPendingJoinRequests = <ThrowOnError extends boolean = false>(options: Options<ListPendingJoinRequestsData, ThrowOnError>) => (options.client ?? client).get<ListPendingJoinRequestsResponses, ListPendingJoinRequestsErrors, ThrowOnError>({
    requestValidator: async (data) => await zListPendingJoinRequestsData.parseAsync(data),
    responseValidator: async (data) => await zListPendingJoinRequestsResponse.parseAsync(data),
    url: '/api/{version}/meetings/{id}/join-requests',
    ...options
});

/**
 * Receive a Resend inbound email webhook
 *
 * Verifies the Svix signature from the request headers against the raw body and, if valid, processes the iMIP calendar reply.
 */
export const handleInboundEmail = <ThrowOnError extends boolean = false>(options: Options<HandleInboundEmailData, ThrowOnError>) => (options.client ?? client).post<HandleInboundEmailResponses, HandleInboundEmailErrors, ThrowOnError>({
    requestValidator: async (data) => await zHandleInboundEmailData.parseAsync(data),
    url: '/api/{version}/webhooks/resend/inbound',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Subscribe to a join request's decision
 *
 * Opens a text/event-stream connection scoped by request id that delivers the host's accept/decline outcome as a join_request_approved (token, roomName) or join_request_denied (reason) event, replays a decision already recorded before subscribe, and sends periodic heartbeat comments (`: ka`) until the configured timeout. Each event is sent as an SSE frame with an `event:` line (either `join_request_approved` or `join_request_denied`) followed by a `data:` line containing the JSON payload described in the schema.
 */
export const subscribeRequest = <ThrowOnError extends boolean = false>(options: Options<SubscribeRequestData, ThrowOnError>) => (options.client ?? client).sse.get<SubscribeRequestResponses, SubscribeRequestErrors, ThrowOnError>({
    requestValidator: async (data) => await zSubscribeRequestData.parseAsync(data),
    responseValidator: async (data) => await zSubscribeRequestResponse.parseAsync(data),
    url: '/api/{version}/meetings/{id}/join-requests/{requestId}/events',
    ...options
});

/**
 * Subscribe to a meeting's join events
 *
 * Opens a text/event-stream connection that delivers join_request_created events for the meeting, replays currently pending requests on subscribe, and sends periodic heartbeat comments (`: ka`) until the configured timeout. Each event is sent as an SSE frame with an `event: join_request_created` line followed by a `data:` line containing the JSON payload described in the schema.
 */
export const subscribe = <ThrowOnError extends boolean = false>(options: Options<SubscribeData, ThrowOnError>) => (options.client ?? client).sse.get<SubscribeResponses, SubscribeErrors, ThrowOnError>({
    requestValidator: async (data) => await zSubscribeData.parseAsync(data),
    responseValidator: async (data) => await zSubscribeResponse.parseAsync(data),
    url: '/api/{version}/meetings/{id}/events',
    ...options
});
