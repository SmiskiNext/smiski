## [2026-04-12] Round 1 (from opsx-apply auto-verify)

### opsx-arch-verifier

- Fixed: [C3] Silent error swallowing in
  `LoginFragment.handleGoogleCredential()` — added `showGeneralError()` call in
  catch block so user gets feedback when Google credential parsing fails
- Fixed: [C4] Unmanaged `Executors.newSingleThreadExecutor()` per invocation —
  replaced with single `ExecutorService` field in both `LoginViewModel` and
  `RegisterViewModel`, added `onCleared()` shutdown
- Fixed: [W3] Null check on Firebase ID token — added null guard before calling
  `viewModel.loginWithGoogle()` in `LoginFragment`
- Skipped: [C1] TokenManager imported in LoginViewModel (presentation → data
  violation) — pre-existing project convention, requires broader refactoring
  discussion
- Skipped: [C2] ApiFailException/ApiErrorException imported in ViewModels
  (presentation → data violation) — explicitly documented in UiError.java
  Javadoc as the project's error-mapping convention, predates this change
- Skipped: [W1] Duplicated error-mapping boilerplate — low risk, extraction to
  shared utility is a separate refactoring task
- Skipped: [W2] AuthActivity start-destination override creates duplicate
  backstack — acceptable UX (back from register → login), architecture
  improvement deferred
- Skipped: [S1] MasterKeys deprecated API — we use security-crypto:1.1.0
  (stable) which documents MasterKeys as the correct API
- Skipped: [S2] Hardcoded UI strings — i18n is a separate initiative, not in
  scope

## [2026-04-12] Round 2 (build verification)

### Build errors

- Fixed: Missing `import io.github.phunguy65.zms.domain.model.LoginResult` in
  `LoginFragment.java` — added import for pattern matching in switch
- Fixed: Wrong `R` class reference for `default_web_client_id` — changed from
  `com.google.firebase.auth.R.string` to `R.string` (generated by
  google-services plugin into app's own R)
- Result: `./gradlew :app:assembleDebug` BUILD SUCCESSFUL (42 tasks, 0 errors)
